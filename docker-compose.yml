services:
  claude-gateway:
    build:
      context: ./openresty
      dockerfile: Dockerfile
    image: claude-gateway:latest
    container_name: claude-gateway

    # 环境变量配置
    environment:
      # API Token（必须设置，建议至少32字符）
      - API_TOKEN=${API_TOKEN:-default-secret-token-please-change-me}
      # 是否启用动态路由（true/false，默认false）
      - ENABLE_DYNAMIC_ROUTING=${ENABLE_DYNAMIC_ROUTING:-false}
      # 上游服务地址（ENABLE_DYNAMIC_ROUTING=false时使用）
      - UPSTREAM_URL=${UPSTREAM_URL:-https://api.anthropic.com}
      # OpenAI/Codex 上游服务地址（/openai* 默认模式使用）
      - OPENAI_UPSTREAM_URL=${OPENAI_UPSTREAM_URL:-https://api.openai.com/v1}
      # OpenResty worker 数量，纯 Lua 关键词方案默认建议 1
      - WORKER_PROCESSES=${WORKER_PROCESSES:-1}
      # 关键词分块构建大小，用于降低大词库启动峰值内存
      - KEYWORD_CHUNK_SIZE=${KEYWORD_CHUNK_SIZE:-50000}

    # 端口映射
    ports:
      - "${HOST_IP:-0.0.0.0}:${HOST_PORT:-80}:80"

    # 卷挂载（可选）
    volumes:
      # 持久化配置目录（包含 keywords.txt 和 routes.txt）
      - ${CONFIG_DIR:-./openresty}:/etc/openresty
      # 持久化日志（可选）
      # - ./logs:/usr/local/openresty/nginx/logs

    # 健康检查
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 300s

    # 重启策略
    restart: unless-stopped
    # 资源限制（可选）
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 256M

    # 日志配置
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

    # 网络配置
    networks:
      - claude-gateway-network

# 网络定义
networks:
  claude-gateway-network:
    name: ${DOCKER_NETWORK_NAME:-claude-gateway-network}
    driver: ${DOCKER_NETWORK_DRIVER:-bridge}
    external: ${DOCKER_NETWORK_EXTERNAL:-false}
